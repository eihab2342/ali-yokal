# دليل ومواصفات الـ Backend API المطلوب لعيادة الأسنان (Dental Clinic)

هذا الملف موجه لمبرمج الـ Backend (Laravel) لتوضيح الحقول ونقاط النهاية (Endpoints) المطلوبة لربط الموقع بالكامل بالداشبورد وخاصة **معرض الحالات قبل وبعد (Before & After Slider)** وكافة أقسام العيادة.

---

## 1. قسم الحالات وسلايدر "قبل وبعد" (Before & After Cases)

يمكنك توفيرها إما عبر تخصيص جدول وموديل الـ `projects` الحالي أو إنشاء موديل خاص باسم `cases` / `before_after`.

### البيانات المطلوبة لكل حالة (Case Object Fields):
| الحقل (Field) | النوع (Type) | الوصف | مثال |
|---|---|---|---|
| `id` | `integer` | المعرف الفريد للحالة | `1` |
| `name` / `title` | `string` | عنوان الحالة الطبية | `"حشو تجميلي أمامي متعدد الطبقات"` |
| `type` / `tag` | `string` | تصنيف الحالة / التخصص | `"حشو تجميلي كومبوزيت"` / `"تغطية عصب"` / `"فينيرز"` |
| `subtitle` | `string` (اختياري) | عنوان فرعي أو ملخص سريع | `"ترميم كسر وتآكل الأسنان بدون برد"` |
| `before_image` | `string (URL)` | رابط صورة الحالة **قبل** العلاج | `"https://.../before.jpg"` |
| `after_image` | `string (URL)` | رابط صورة الحالة **بعد** العلاج | `"https://.../after.jpg"` |
| `description` | `text` | تفاصيل وشرح الإجراء الطبي | `"تم عزل السن بالكامل بالـ Rubber Dam وإعادة بناء طبقات المينا والعاج..."` |
| `sort_order` | `integer` | ترتيب ظهور الحالة | `1` |

### إذا تم استخدام جدول `projects` الحالي:
يجب أن تحتوي كل حالة في مصفوفة `images` على صورتين على الأقل:
- الصورة الأولى `images[0].image_url` تمثل **صورة قبل (Before)**.
- الصورة الثانية `images[1].image_url` تمثل **صورة بعد (After)**.
- حقل `type` يمثل التخصص (مثل: علاج تحفظي، فينيرز، حشو تجميلي).

---

## 2. الإحصائيات (Statistics)
يجب أن ترجع الإحصائيات الخاصة بالعيادة مثل:
```json
[
  { "id": 1, "title": "حالة علاجية ناجحة", "count": 5000 },
  { "id": 2, "title": "سنة خبرة تخصصية", "count": 15 },
  { "id": 3, "title": "أبحاث ودراسات منشورة", "count": 12 },
  { "id": 4, "title": "ساعة تدريب وتعليم مستمر", "count": 25000 }
]
```

---

## 3. الخدمات التخصصية (Services)
كل خدمة تحتاج إلى الحقول التالية:
```json
{
  "id": 1,
  "name": "الحشوات التجميلية الميكرونية",
  "short_desc": "ترميم الأسنان الأمامية والخلفية بتقنيات الحشو الميكروني متعدد الطبقات للحفاظ على السن الطبيعي.",
  "image_url": "https://...",
  "alt_image": "حشو تجميلي"
}
```

---

## 4. خطوات وبروتوكولات العلاج (Sections / Step by Step)
الأقسام الخاصة بـ:
- **معايير العناية والتفرد (`standards`)**:
  - `01` جلسة VIP مخصصة (ساعة كاملة لكل مريض).
  - `02` عزل تام بحاجز العزل المطاطي Rubber Dam.
  - `03` إنقاذ حيوية العصب (Pulp Capping).
- **خطوات العلاج خطوة بخطوة (`step_by_step`)**:
  - `01` المسح الرقمي ثلاثي الأبعاد والتشخيص المجهري.
  - `02` وضع خطة العلاج التحفظي الدقيق.
  - `03` التنفيذ الميكروني والمحاكاة الطبيعية.
  - `04` المتابعة والضمان الدوري.

---

## 5. هيكل استجابة الـ API الكامل (`GET /api/home`)

```json
{
  "status": 200,
  "message": "Success",
  "data": {
    "home": {
      "description": "دكتوراه في العلاج التحفظي وتجميل الأسنان - دقة ميكرونية وعناية فردية فائقة",
      "logo": "https://.../logo.png"
    },
    "about": {
      "title": "فلسفتنا الطبية: الحفاظ على أسنانك الطبيعية بأعلى دقة",
      "description": "نؤمن في عيادتنا بأن أفضل سن هو سنك الطبيعي. نركز على العلاج التحفظي الدقيق وإنقاذ العصب الحي بأحدث تقنيات العزل والمسح الرقمي ثلاثي الأبعاد."
    },
    "statistics": [ ... ],
    "services": [ ... ],
    "projects": [
      {
        "id": 1,
        "name": "حشو تجميلي أمامي متعدد الطبقات",
        "short_desc": "ترميم وتجميل الأسنان الأمامية بدون أي برد للأسنان السليمة.",
        "type": "حشو تجميلي كومبوزيت",
        "thumbnail_url": "https://.../after.jpg",
        "images": [
          { "id": 1, "image_url": "https://.../case1_before.jpg", "alt": "Before" },
          { "id": 2, "image_url": "https://.../case1_after.jpg", "alt": "After" }
        ]
      }
    ],
    "sections": {
      "standards": [ ... ],
      "step_by_step": [ ... ],
      "quality_control": [ ... ],
      "project_risks": [ ... ]
    },
    "contact": {
      "phone": "+201XXXXXXXXX",
      "email": "info@example.com",
      "address": "الإسكندرية - سبورتنج"
    },
    "social_links": [ ... ]
  }
}
```

---

## 6. ملاحظات للـ Backend Developer:
1. **الصور ومساراتها**: يُرجى التأكد من أن مسارات الصور تُرجع روابط كاملة تبدأ بـ `http://` أو `https://`، أو مسارات نسبية واضحة يتم تحويلها تلقائياً بـ `Storage::url()`.
2. **تعدد اللغات (Localization)**: يتم إرسال هيدر `Accept-Language: ar` أو `Accept-Language: en` أو عبر باراميتر في الـ URL للحصول على النصوص باللغة العربية أو الإنجليزية.
